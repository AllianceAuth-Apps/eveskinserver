[flake8]
exclude = .git, *migrations*, .tox, dist, htmlcov
max-line-length = 88
select = C,E,F,W,B,B950
ignore = E203, E231, E501, W503, W291, W293

[tox]
envlist = py37

[testenv]
usedevelop = true
deps=
    coverage

commands=
    coverage run tests/test_all.py -v 2
    coverage report
